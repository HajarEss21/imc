name: imc_secured
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  firebase_ui_localizations: ^1.4.0
  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.5
  provider: ^6.1.2
  firebase_ui_auth: ^1.16.1
  go_router: ^14.8.1
  google_fonts: ^6.2.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  intl_utils: ^2.0.0
  flutter_lints: ^5.0.0

flutter:
  generate: true  # Enable automatic generation of localization files
  uses-material-design: true
